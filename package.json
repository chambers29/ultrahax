{
  "name": "ultrahax",
  "version": "1.0.0",
  "private": true,
  "description": "Utilities for generating HaxBall stadium files from YAML templates.",
  "scripts": {
    "generate:stadium": "node stadium_generator.js",
    "headless:build": "node headless/build.js"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}