[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ultrahax"
version = "0.1.0"
description = "Pipeline runner"
requires-python = ">=3.11"

[project.scripts]
run-pipeline = "ultrahax.main:main"

[tool.setuptools.packages.find]
where = ["src"]